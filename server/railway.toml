[build]
builder = "RAILPACK"

[deploy]
startCommand = "node --no-deprecation --import tsx/esm server/src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
