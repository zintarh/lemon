FROM node:20-alpine
WORKDIR /app

# Copy entire monorepo so npm can resolve workspaces
COPY . .

# Install with full workspace context (all package.json files present)
RUN npm install

EXPOSE 3000
CMD ["node", "--no-deprecation", "--import", "tsx/esm", "server/src/index.ts"]
