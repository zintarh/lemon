FROM node:20-alpine
WORKDIR /app

# Copy entire monorepo so npm can resolve workspaces
COPY . .

# Install with full workspace context (all package.json files present)
RUN npm install

EXPOSE 8080
CMD ["node", "--no-deprecation", "--import", "tsx/esm", "agent/src/index.ts"]
